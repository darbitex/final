/// Type stub for `dex_contract::pool_v3::LiquidityPoolV3` — the
/// Hyperion CLMM pool resource. Flashbot holds `Object<LiquidityPoolV3>`
/// handles and never touches fields, so a placeholder layout is fine.
module dex_contract::pool_v3 {
    struct LiquidityPoolV3 has key {
        _dummy: u64,
    }
}
